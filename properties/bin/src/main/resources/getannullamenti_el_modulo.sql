SELECT * 
FROM DMALM_EL_MODULI u1 
WHERE u1.ID_MODULO NOT IN (SELECT ID_MODULO FROM DMALM_STG_EL_MODULI WHERE DT_CARICAMENTO = ?) 
AND DT_FINE_VALIDITA = (SELECT MAX(DT_FINE_VALIDITA) FROM DMALM_EL_MODULI u2 WHERE u1.ID_MODULO=u2.ID_MODULO) 
AND u1.DMALM_MODULO_PK>0 AND u1.ANNULLATO ='NO'
