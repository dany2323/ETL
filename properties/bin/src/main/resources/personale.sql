	SELECT 
	p.DMALM_STG_PERSONALE_PK as DMALM_PERSONALE_PK,
	p.ID as ID_EDMA,
	p.CODICE as CD_PERSONALE,
	TO_DATE(p.DATA_INIZIO_VALIDITA, 'YYYY-MM-DD HH24:MI:SS') as DT_INIZIO_VALIDITA,
	TO_DATE(p.DATA_FINE_VALIDITA, 'YYYY-MM-DD HH24:MI:SS') as DT_FINE_VALIDITA,
	TO_DATE(p.DATA_ATTIVAZIONE, 'YYYY-MM-DD HH24:MI:SS') as DT_ATTIVAZIONE,
	TO_DATE(p.DATA_DISATTIVAZIONE, 'YYYY-MM-DD HH24:MI:SS') as DT_DISATTIVAZIONE,
	p.NOTE as NOTE,
	p.INTERNO as INTERNO,
	NVL(p.COD_RESPONSABILE,0) as CD_RESPONSABILE,
	p.INDIRIZZO_EMAIL as INDIRIZZO_EMAIL,
	p.NOME as NOME,
	p.COGNOME as COGNOME,
	p.MATRICOLA as MATRICOLA,
	p.CODICE_FISCALE as CODICE_FISCALE,
	p.IDENTFICATORE as IDENTIFICATORE,
	p.ID_GRADO_UFFICIO as ID_GRADO,
	p.ID_SEDE as ID_SEDE,
	NVL(p.COD_SUPERIORE,0 ) as CD_SUPERIORE,
	p.COD_ENTE as CD_ENTE,
	p.COD_VISIBILITA as CD_VISIBILITA
 FROM DMALM_STG_PERSONALE p 
WHERE p.DATA_CARICAMENTO = ?