SELECT * 
FROM DMALM_EL_UNITA_ORGANIZZATIVE u1 
WHERE u1.CD_AREA NOT IN (SELECT CD_AREA FROM DMALM_STG_EL_UNITA_ORGANIZZ WHERE DT_CARICAMENTO = ?) 
AND DT_FINE_VALIDITA = (SELECT MAX(DT_FINE_VALIDITA) FROM DMALM_EL_UNITA_ORGANIZZATIVE u2 WHERE u1.CD_AREA=u2.CD_AREA) 
AND DMALM_UNITA_ORG_PK >0 
AND ANNULLATO <>'SI' 
